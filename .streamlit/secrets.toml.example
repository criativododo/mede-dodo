# Copie este arquivo para `.streamlit/secrets.toml` (NUNCA versionar o real —
# ver .gitignore) e preencha os valores. Lido por src/features/coleta/auth.py
# (get_secret), com fallback para variáveis de ambiente / .env quando ausente.

APP_PASSWORD = "sua_senha_aqui"
GEMINI_API_KEY = "sua_chave_gemini"
